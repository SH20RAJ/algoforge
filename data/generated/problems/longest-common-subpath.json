{
  "id": "1923",
  "number": 1923,
  "slug": "longest-common-subpath",
  "title": "Longest Common Subpath",
  "difficulty": "Hard",
  "topics": [
    "hash-table"
  ],
  "patterns": [
    "binary-search",
    "rabin-karp-algorithm"
  ],
  "companies": [],
  "languages": [
    "cpp",
    "python"
  ],
  "timeComplexity": "O(m * nlogn)",
  "spaceComplexity": "O(n)",
  "premium": false,
  "leetcodeUrl": "https://leetcode.com/problems/longest-common-subpath/",
  "notes": [
    "Binary Search",
    "Rabin-Karp Algorithm"
  ],
  "relatedSlugs": [
    "lexicographically-smallest-string-after-reverse-ii",
    "smallest-unique-subarray",
    "k-divisible-elements-subarrays",
    "lexicographically-smallest-string-after-reverse",
    "substring-with-concatenation-of-all-words",
    "minimum-window-substring",
    "max-points-on-a-line",
    "longest-substring-with-at-most-two-distinct-characters"
  ],
  "prevSlug": "count-good-numbers",
  "nextSlug": "erect-the-fence-ii",
  "estimatedMinutes": 50,
  "content": [
    {
      "id": "beginner",
      "title": "Beginner Explanation",
      "status": "ready",
      "bodyMarkdown": "## What is Longest Common Subpath?\n\n**Longest Common Subpath** (LeetCode #1923) is a **Hard** problem that primarily trains **hash table**.\n\n### How to think about it\n\n1. **Restate the goal** in your own words before coding.\n2. Work a **tiny example** by hand so the invariant becomes obvious.\n3. Identify the **pattern** — this problem aligns with **binary search and rabin karp algorithm**.\n4. Only then translate the idea into code.\n\n### Why this problem matters\n\nHard problems force you to combine patterns and prove complexity carefully — interview gold.\n Official solution notes mention: Binary Search, Rabin-Karp Algorithm.\n\n> AlgoForge explanations are original teaching notes. Always open the [official problem statement on LeetCode](https://leetcode.com/problems/longest-common-subpath/) for constraints and examples.\n"
    },
    {
      "id": "interview",
      "title": "Interview Walkthrough",
      "status": "ready",
      "bodyMarkdown": "## Interview approach for Longest Common Subpath\n\n### Opening (30–60 seconds)\n\n- Clarify inputs/outputs and edge cases (empty input, single element, duplicates, overflow).\n- State a **brute force** so the interviewer knows you can solve it naively.\n- Propose the **optimal direction** tied to **binary search and rabin karp algorithm**.\n\n### Core solution narrative\n\n1. Define the **state** you track (pointers, DP cell, set membership, stack top, etc.).\n2. Explain the **transition** when you process the next element.\n3. Call out **time** (O(m * nlogn)) and **space** (O(n)) before coding.\n4. Code cleanly; narrate variable names.\n\n### What interviewers listen for\n\n- Correctness on edge cases\n- Complexity honesty\n- Ability to discuss **trade-offs** (e.g., hash map space vs. sort + two pointers)\n\n### Follow-up questions they may ask\n\n- Can you solve it with less memory?\n- What if the input stream is infinite / doesn't fit in RAM?\n- How would tests look for adversarial inputs?\n"
    },
    {
      "id": "optimized",
      "title": "Optimized Approach",
      "status": "ready",
      "bodyMarkdown": "## Optimized solution notes\n\nThe reference solutions on AlgoForge target **O(m * nlogn)** time and **O(n)** space.\n\n### Pattern focus: binary search and rabin karp algorithm\n\nUse the pattern as a checklist:\n\n- **binary search** — confirm the invariant holds after each step\n- **rabin karp algorithm** — confirm the invariant holds after each step\n\nMultiple methods appear in the source solutions — compare them and explain when each is preferable.\n\n### Implementation tips\n\n- Prefer readable names over micro-optimizations in interviews.\n- Extract helpers only when they clarify (e.g., expand-around-center, DFS visit).\n- After AC-level logic, re-scan for off-by-one and null checks.\n"
    },
    {
      "id": "complexity",
      "title": "Complexity Analysis",
      "status": "ready",
      "bodyMarkdown": "## Complexity\n\n| Measure | Bound |\n|--------|-------|\n| Time | **O(m * nlogn)** |\n| Space | **O(n)** |\n\n### How to justify this in an interview\n\n- **Time:** count loops, map/set operations, and recursive branching; state average vs worst case if relevant.\n- **Space:** include hash maps, recursion stack, and output allocation when the problem asks for it.\n\nIf your implementation differs from the reference, re-derive big-O from *your* code — never memorize a complexity you cannot defend.\n"
    },
    {
      "id": "common-mistakes",
      "title": "Common Mistakes",
      "status": "ready",
      "bodyMarkdown": "## Common mistakes on Longest Common Subpath\n\n1. **Skipping edge cases** — empty collections, single-element inputs, max constraints.\n2. **Wrong invariant** for binary search and rabin karp algorithm — updating state too early or too late.\n3. **Mutating input unexpectedly** when the problem forbids it.\n4. **Off-by-one** in windows, ranges, or binary search bounds.\n5. **Ignoring overflow / precision** for integer arithmetic problems.\n6. **Overengineering** — jumping to an advanced structure when a simpler approach works.\n"
    },
    {
      "id": "alternatives",
      "title": "Alternative Approaches",
      "status": "ready",
      "bodyMarkdown": "## Alternatives\n\nThe source file includes more than one method. Compare:\n\n1. **Primary optimized path** — best complexity for typical interviews.\n2. **Secondary approach** — often brute force, sorting-based, or space-optimized variant.\n\nPractice articulating **when** you would pick each (constraints, readability, follow-ups).\n"
    },
    {
      "id": "edge-cases",
      "title": "Edge Cases",
      "status": "ready",
      "bodyMarkdown": "## Edge cases checklist\n\n- Minimum input size\n- Maximum input size / time limits\n- Duplicates and already-sorted input\n- Negative numbers / zeros (if applicable)\n- Disconnected structures (graphs/trees)\n- Single path vs branching recursion depth\n"
    },
    {
      "id": "pattern-recognition",
      "title": "Pattern Recognition",
      "status": "ready",
      "bodyMarkdown": "## Spotting this pattern\n\nSignal phrases that point to **binary search and rabin karp algorithm**:\n\n- Sorted input or ability to sort without changing the answer class\n- Need for contiguous subarray / substring → consider sliding window\n- Need for O(1) membership → hash set/map\n- Optimal substructure + overlapping subproblems → DP\n- Connectivity / components → graph DFS/BFS or Union-Find\n\nPrimary topics: **hash table**.\n"
    },
    {
      "id": "follow-ups",
      "title": "Follow-up Interview Questions",
      "status": "ready",
      "bodyMarkdown": "## Follow-ups\n\n1. How does the solution change if the input is a stream?\n2. Can you solve it in-place?\n3. What if duplicates must be handled differently?\n4. How would you parallelize the approach?\n5. Design tests that would break a buggy implementation.\n"
    },
    {
      "id": "practice",
      "title": "Practice Recommendations",
      "status": "ready",
      "bodyMarkdown": "## What to practice next\n\n1. Re-solve **Longest Common Subpath** in a second language (cpp, python).\n2. Drill 3–5 more problems tagged **hash table**.\n3. Teach the solution out loud in under 5 minutes.\n4. Add this problem to your revision calendar in 3 days and 14 days.\n"
    }
  ],
  "solutions": {
    "cpp": {
      "language": "C++",
      "languageSlug": "cpp",
      "filename": "longest-common-subpath.cpp",
      "code": "// Time:  O(m * nlogn)\n// Space: O(n)\n\nclass Solution {\npublic:\n    int longestCommonSubpath(int n, vector<vector<int>>& paths) {\n        int left = 1, right = size(*min_element(cbegin(paths), cend(paths),\n                                                [](const auto& x, const auto& y) {\n                                                    return size(x) < size(y);\n                                                }));\n        while (left <= right) {\n            const auto& mid = left + (right - left) / 2;\n            if (!check(paths, mid)) {\n                right = mid - 1;\n            } else {\n                left = mid + 1;\n            }\n        }\n        return right;\n    }\n\nprivate:\n    template<typename T>\n    struct VectorHash {\n        size_t operator()(const std::vector<T>& v) const {\n            size_t seed = 0;\n            for (const auto& i : v) {\n                seed ^= std::hash<T>{}(i)  + 0x9e3779b9 + (seed<<6) + (seed>>2);\n            }\n            return seed;\n        }\n    };\n\n    bool check(const vector<vector<int>>& paths, int x) {\n        unordered_set<vector<int>, VectorHash<int>> intersect = RabinKarp(paths[0], x);\n        for (int i = 1; i < size(paths); ++i) {\n            intersect = set_intersection<vector<int>, VectorHash<int>>(intersect, RabinKarp(paths[i], x));\n            if (empty(intersect)) {\n                return false;\n            }\n        }\n        return true;\n    }\n    \n    unordered_set<vector<int>, VectorHash<int>> RabinKarp(const vector<int>& arr, int x) {\n        static const int MOD = 1e9 + 7;  // MOD could be the min prime of 7-digit number (1e6+3)\n        static const vector<int> P = {113, 109};  // double hashing, P could be {2, 3}\n        vector<int> hashes, powers;\n        for (const auto& p : P) {\n            int64_t h = 0, power = 1;\n            for (int i = 0; i < x; ++i) {\n                h = (h * p + arr[i]) % MOD;\n                power = (power * p) % MOD;\n            }\n            hashes.emplace_back(h);\n            powers.emplace_back(power);\n        }\n        unordered_set<vector<int>, VectorHash<int>> lookup = {hashes};\n        for (int i = x; i < size(arr); ++i) {\n            for (int j = 0; j < size(P); ++j) {\n                hashes[j] = ((int64_t(hashes[j]) * P[j] - int64_t(arr[i - x]) * powers[j] + arr[i]) % MOD + MOD) % MOD;\n            }\n            lookup.emplace(hashes);\n        }\n        return lookup;\n    }\n\n    template<typename T, typename H>\n    unordered_set<T, H> set_intersection(const unordered_set<T, H>& a,\n                                         const unordered_set<T, H>& b) {\n        if (a.size() > b.size()) {\n            return set_intersection(b, a);\n        }\n        unordered_set<T, H> result;\n        for (const auto& x : a) {\n            if (b.count(x)) {\n                result.emplace(x);\n            }\n        }\n        return result;\n    }\n};\n\n// Time:  O(m * nlogn)\n// Space: O(n)\nclass Solution2 {\npublic:\n    int longestCommonSubpath(int n, vector<vector<int>>& paths) {\n        int left = 1, right = size(*min_element(cbegin(paths), cend(paths),\n                                                [](const auto& x, const auto& y) {\n                                                    return size(x) < size(y);\n                                                }));\n        while (left <= right) {\n            const auto& mid = left + (right - left) / 2;\n            if (!check(paths, mid)) {\n                right = mid - 1;\n            } else {\n                left = mid + 1;\n            }\n        }\n        return right;\n    }\n\nprivate:\n    bool check(const vector<vector<int>>& paths, int x) {\n        unordered_set<int64_t> intersect = RabinKarp(paths[0], x);\n        for (int i = 1; i < size(paths); ++i) {\n            intersect = set_intersection<int64_t>(intersect, RabinKarp(paths[i], x));\n            if (empty(intersect)) {\n                return false;\n            }\n        }\n        return true;\n    }\n    \n    unordered_set<int64_t> RabinKarp(const vector<int>& arr, int x) {\n        static const int64_t MOD = 1e11 + 19;  // MOD is the min prime of 12-digit number\n        static const int64_t P = 1e5 + 1;  // max(x for p in paths for x in p)+1\n        int64_t h = 0, power = 1;\n        for (int i = 0; i < x; ++i) {\n            h = (h * P + arr[i]) % MOD;\n            power = (power * P) % MOD;\n        }\n        unordered_set<int64_t> lookup = {h};\n        for (int i = x; i < size(arr); ++i) {\n            h = ((h * P - arr[i - x] * power + arr[i]) % MOD + MOD) % MOD;\n            lookup.emplace(h);\n        }\n        return lookup;\n    }\n\n    template<typename T>\n    unordered_set<T> set_intersection(const unordered_set<T>& a,\n                                      const unordered_set<T>& b) {\n        if (a.size() > b.size()) {\n            return set_intersection(b, a);\n        }\n        unordered_set<T> result;\n        for (const auto& x : a) {\n            if (b.count(x)) {\n                result.emplace(x);\n            }\n        }\n        return result;\n    }\n};\n\n",
      "sourcePath": "C++/longest-common-subpath.cpp"
    },
    "python": {
      "language": "Python",
      "languageSlug": "python",
      "filename": "longest-common-subpath.py",
      "code": "# Time:  O(m * nlogn)\n# Space: O(n)\n\nclass Solution(object):\n    def longestCommonSubpath(self, n, paths):\n        \"\"\"\n        :type n: int\n        :type paths: List[List[int]]\n        :rtype: int\n        \"\"\"\n        def RabinKarp(arr, x):  # double hashing\n            hashes = tuple([reduce(lambda h,x: (h*p+x)%MOD, (arr[i] for i in xrange(x)), 0) for p in P])\n            powers = [pow(p, x, MOD) for p in P]\n            lookup = {hashes}\n            for i in xrange(x, len(arr)):\n                hashes = tuple([(hashes[j]*P[j] - arr[i-x]*powers[j] + arr[i])%MOD for j in xrange(len(P))])  # in smaller datasets, tuple from list is much faster than tuple from generator, see https://stackoverflow.com/questions/16940293/why-is-there-no-tuple-comprehension-in-python\n                lookup.add(hashes)\n            return lookup\n        \n        def check(paths, x):\n            intersect = RabinKarp(paths[0], x)\n            for i in xrange(1, len(paths)):\n                intersect = set.intersection(intersect, RabinKarp(paths[i], x))\n                if not intersect:\n                    return False\n            return True\n\n        MOD, P = 10**9+7, (113, 109)  # MOD could be the min prime of 7-digit number (10**6+3), P could be (2, 3)\n        left, right = 1, min(len(p) for p in paths)\n        while left <= right:\n            mid = left + (right-left)//2\n            if not check(paths, mid):\n                right = mid-1\n            else:\n                left = mid+1\n        return right\n\n\n# Time:  O(m * nlogn)\n# Space: O(n)\nclass Solution2(object):\n    def longestCommonSubpath(self, n, paths):\n        \"\"\"\n        :type n: int\n        :type paths: List[List[int]]\n        :rtype: int\n        \"\"\"\n        def RabinKarp(arr, x):\n            h = reduce(lambda h,x: (h*P+x)%MOD, (arr[i] for i in xrange(x)), 0)\n            power = pow(P, x, MOD)\n            lookup = {h}\n            for i in xrange(x, len(arr)):\n                h = (h*P - arr[i-x]*power + arr[i])%MOD\n                lookup.add(h)\n            return lookup\n        \n        def check(paths, x):\n            intersect = RabinKarp(paths[0], x)\n            for i in xrange(1, len(paths)):\n                intersect = set.intersection(intersect, RabinKarp(paths[i], x))\n                if not intersect:\n                    return False\n            return True\n\n        MOD, P = 10**11+19, max(x for p in paths for x in p)+1  # MOD is the min prime of 12-digit number\n        left, right = 1, min(len(p) for p in paths)\n        while left <= right:\n            mid = left + (right-left)//2\n            if not check(paths, mid):\n                right = mid-1\n            else:\n                left = mid+1\n        return right\n",
      "sourcePath": "Python/longest-common-subpath.py"
    }
  },
  "seo": {
    "title": "Longest Common Subpath Solution — Hard | AlgoForge",
    "description": "Original Hard explanation for Longest Common Subpath (LeetCode #1923). Pattern: binary search. Time O(m * nlogn), space O(n). Multi-language solutions and interview tips.",
    "keywords": [
      "Longest Common Subpath",
      "Longest Common Subpath solution",
      "Longest Common Subpath leetcode",
      "hash-table",
      "binary-search",
      "rabin-karp-algorithm",
      "hard"
    ],
    "canonicalPath": "/problems/longest-common-subpath"
  },
  "faqs": [
    {
      "question": "What is the time complexity of Longest Common Subpath?",
      "answer": "The reference solutions aim for O(m * nlogn) time and O(n) space. Always re-derive complexity from the code you write in the interview."
    },
    {
      "question": "What pattern does Longest Common Subpath use?",
      "answer": "It primarily maps to binary search and rabin karp algorithm, within the broader topic of hash table."
    },
    {
      "question": "Is Longest Common Subpath good for interviews?",
      "answer": "Yes — as a Hard problem it is a solid practice target. Pair it with related problems in the same pattern family for spaced repetition."
    },
    {
      "question": "Where can I read the official statement?",
      "answer": "Open the official LeetCode page for constraints and examples: https://leetcode.com/problems/longest-common-subpath/"
    }
  ],
  "studyChecklist": [
    "Read the official problem statement on LeetCode",
    "Solve on paper / whiteboard first",
    "Implement the binary search and rabin karp algorithm approach",
    "Verify edge cases from the checklist",
    "State time and space complexity aloud",
    "Compare with the AlgoForge reference solution",
    "Schedule a revision session"
  ],
  "revisionNotes": "Longest Common Subpath (#1923) — Hard. Pattern: binary search and rabin karp algorithm. Complexity: O(m * nlogn) time / O(n) space. Re-derive the invariant before coding.",
  "visualizationDescription": "Conceptual diagram for Longest Common Subpath: show input structure (hash table), highlight the moving parts of the binary search and rabin karp algorithm approach, and annotate each step with the maintained invariant and complexity."
}